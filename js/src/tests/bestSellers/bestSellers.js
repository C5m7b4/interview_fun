import { sales, items, departments } from '../../data';
import { Box } from '../../Box';

const salesSorter = (a, b) => {};

const unique = (items) => {};

// make this object the 2 highest selling items
// from 7/1 to 8/1
export const bestSellers = {};

export const bestSellersBonus = {};
