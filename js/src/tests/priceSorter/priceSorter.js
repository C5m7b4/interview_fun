import { items } from '../../data';
import { Box } from '../../Box';

// you can use this helper function to make sorting a litte less crowdy 🙈
export const priceSorter = (a, b) => {};

// make this function return all the items sorted from highest priced to lowest priced 🤿

export const sortedItems = {};
